# Backend environment variables
# Set to 'false' to use sqlite persistence instead of in-memory fakes
USE_FAKES=true
DEV_SKIP_CPF_VALIDATION=false

# SQLite file path (used when USE_FAKES=false)
SQLITE_FILE=./dev.sqlite
